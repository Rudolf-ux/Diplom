export default function Navbar() {
  return (
    <header className="header">
      <div>DropZone</div>

      <nav className="nav">
        <a href="#">Cases</a>
        <a href="#">Telegram</a>
        <a href="#">Discord</a>
        <a href="#">FAQ</a>
      </nav>

      <button>Login</button>
    </header>
  );
}
